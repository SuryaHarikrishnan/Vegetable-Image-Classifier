# Vegetable-Image-Classifier
# Vegetable Image Classifier (PyTorch)

A convolutional neural network built with PyTorch that classifies vegetable images.

## Features

- CNN architecture
- PyTorch DataLoader pipeline
- Training + validation loop
- PyTest unit tests
- Binary classification (carrot vs sweetcorn)

## Dataset Structure

data/
    train/
    validation/
    test/

## Install

pip install -r requirements.txt

## Run

pytest
